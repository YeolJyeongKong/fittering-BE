package fittering.mall.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import fittering.mall.domain.entity.Outer;

public interface OuterRepository extends JpaRepository<Outer, Long> {
}
